<!DOCTYPE html>
<html>
  <head>
    <title>Portal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'sticker', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="page">
      <%= yield %>
    </div>
  </body>
</html>
