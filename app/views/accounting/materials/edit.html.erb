<% provide :page_title, "Материальные запасы" %>

<%= page_title @accounting.name, [:accounting, @accounting] %>

<div class="row justify-content-center mt-4">
  <div class="col-lg-6">
    <div class="app-card app-card-settings shadow-sm p-4">
      <div class="app-card-body">
        <h4 class="card-title mb-3 d-flex">
          <span>Редактирование маретриального запаса</span>
          <%= link_to bs_icon('trash'), [:accounting, @accounting], class: "ms-auto", data: { turbo_method: :delete, turbo_confirm: t(:confirm) } unless @accounting.code.present? %>
        </h4>

        <%= render 'form', material: @accounting %>
      </div>
    </div>
  </div>
</div>