<div class="row g-3 mb-4 align-items-center justify-content-between">
  <div class="col-auto">
    <h1 class="app-page-title mb-0">[#<%= @asset.uid %>] <%= @asset.name %></h1>
  </div>
  <div class="col-auto">
    <upload-files  class="btn app-btn-primary" url="<%= accounting_asset_path(@asset, format: :json) %>" param="asset[images][]" accept="image/*" multiple>
      <svg width="1em" height="1em" viewBox="0 0 16 16" class="bi bi-upload me-2" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
        <path fill-rule="evenodd" d="M.5 9.9a.5.5 0 0 1 .5.5v2.5a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-2.5a.5.5 0 0 1 1 0v2.5a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2v-2.5a.5.5 0 0 1 .5-.5z"></path>
        <path fill-rule="evenodd" d="M7.646 1.146a.5.5 0 0 1 .708 0l3 3a.5.5 0 0 1-.708.708L8.5 2.707V11.5a.5.5 0 0 1-1 0V2.707L5.354 4.854a.5.5 0 1 1-.708-.708l3-3z"></path>
      </svg>Загрузить фото ОС
    </upload-files>
  </div>
</div>

<Fancybox class="row g-4">
  <% @asset.images.each do |image| %>
    <%= image_card_tag image, class: "col-6 col-md-4 col-xl-3 col-xxl-2", deleted: delete_image_attachment_accounting_asset_path(image.signed_id) %>
  <% end %>
</Fancybox>

<hr class="mb-4">

<div class="row g-4 settings-section">
  <div class="col-12 col-md-4">
    <h3 class="section-title">Данные инвентарного учета</h3>
    <div class="section-intro">Здесь представлены основные данные инвентарного учета</div>
  </div>
  <div class="col-12 col-md-8">
    <div class="app-card app-card-settings shadow-sm p-4">
      <div class="app-card-body">
        <div class="mb-2"><strong>Наименование:</strong> <%= @asset.name %></div>
        <div class="mb-2"><strong>Инвентарный номер:</strong> <%= @asset.inventory_number %></div>
        <div class="mb-2"><strong>Балансовая стоимость:</strong> <%= number_to_currency(@asset.total, separator: ',', unit: '₽') %></div>
        <div class="mb-2"><strong>Дата ввода в эксплуатацию:</strong> <%= l @asset.date, format: "%e %B %Y год" if @asset.date %></div>
        <div class="mb-3"><strong>Статус:</strong> <%= status_tag @asset.status %></div>
        
        <div class="mb-2"><strong>Материально-ответственное лицо:</strong> <a href="#">Кудрявцев Михаил Витальевич</a></div>
        <div class="mb-4"><strong>Нахождение:</strong> <a href="#">Отдел информационных ресурсов и технологий</a></div>
        <div class="row justify-content-between">
          <div class="col-auto">
            <%= link_to 'Изменить', edit_accounting_asset_path(@asset), class: "btn app-btn-secondary" %>
          </div>
          <%# <div class="col-auto">
            <a class="btn app-btn-secondary" href="#">Cancel Plan</a>
          </div> %>
        </div>
      </div><!--//app-card-body-->
    </div><!--//app-card-->
  </div>
</div>

<p id="notice"><%= notice %></p>

<%= link_to 'Back', accounting_assets_path %>
